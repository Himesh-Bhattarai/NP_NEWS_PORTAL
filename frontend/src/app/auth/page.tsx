'use client';

import AuthPortal from './AuthPortal'; // ← case sensitive!

export default function AuthPage() {
    return (
         < div >
      <AuthPortal />  {/* This is where the AuthPortal will render */ }
    </div >
    )
}